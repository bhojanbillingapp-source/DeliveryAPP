# This app build is pinned to one restaurant outlet.
# Set OUTLET_ID to that restaurant's bbs.clients.client_id (see BILLINGAPP_API)
# before building for a given outlet, then rebuild — Metro/babel inlines this
# at bundle time, it is not read at runtime.
OUTLET_ID=
